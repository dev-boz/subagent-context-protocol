{
  "name": "sub-mcp",
  "version": "0.1.0",
  "description": "Zero-overhead MCP proxy for AI coding agents via subprocess routing",
  "type": "module",
  "bin": {
    "sub-mcp": "./dist/cli.js"
  },
  "main": "./dist/spawner.js",
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "tsc && node --test dist/test.js"
  },
  "engines": {
    "node": ">=18"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/dev-boz/sub-mcp"
  },
  "dependencies": {
    "commander": "^13.1.0",
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0",
    "@types/js-yaml": "^4.0.9"
  }
}
